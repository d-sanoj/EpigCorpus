[project]
name = "edcs-analytics"
version = "0.1.0"
description = "Incremental EDCS scraper with checkpoint resume and JSON/TSV exports"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.1",
    "requests>=2.33.0",
]
